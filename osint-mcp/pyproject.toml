[project]
name = "osint-mcp"
version = "1.0.0"
description = "OSINT MCP SSE server — centralized Telegram OSINT tools"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "telethon>=1.36.0",
    "starlette",
    "uvicorn[standard]",
]
