# 🤖 Jobs

Личный ИИ-ассистент в Telegram.

## Что это

Полноценный Claude Code агент в твоём Telegram. Пишешь задачу — он делает.

```
— Напомни через час купить молоко
— ✅ Напомню в 15:30

— Спарси hh.ru и найди вакансии python
— [ставит библиотеки, пишет парсер, запускает, присылает результат]

— Сделай телеграм бота для заметок
— [создаёт проект, пишет код, тестирует, деплоит]
```

**Это не чат-бот. Это агент с полным доступом к системе внутри контейнера.**

Может ставить пакеты, писать код, запускать скрипты, работать с файлами, ходить в интернет — всё что нужно для выполнения задачи.

## Запуск

```bash
cp .env.example .env   # заполни TG_API_ID, TG_API_HASH, TG_USER_ID
docker-compose up      # введи телефон, код, залогинься в Claude
```

## TODO

- [ ] Голосовые сообщения
- [ ] Интеграция с календарём
- [ ] Больше tools

## Структура

```
src/
├── main.py        # точка входа
├── session.py     # Claude сессия
├── tools/         # кастомные инструменты
└── telegram/      # Telegram клиент
```

## Env

| Переменная | Зачем |
|------------|-------|
| `TG_API_ID` | Telegram API |
| `TG_API_HASH` | Telegram API |
| `TG_USER_ID` | Твой ID |
