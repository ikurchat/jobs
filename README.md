# 🤖 Jobs

Персональный ИИ-ассистент в Telegram с полным доступом к Claude Code.

## 📦 Статус

### Core
- [x] Telegram клиент (Telethon)
- [x] Phone + OTP авторизация
- [x] Claude Code интеграция (SDK)
- [x] Сессии с сохранением контекста
- [x] Персистентность между перезапусками
- [x] Telegraph для длинных ответов
- [x] Docker изоляция

### Tools
| Tool | Описание | Статус |
|------|----------|--------|
| `schedule_task` | Планирование задач на время | ✅ |
| `list_scheduled_tasks` | Список запланированных задач | ✅ |
| `cancel_scheduled_task` | Отмена задачи по ID | ✅ |

### UX
- [x] Typing status при обработке
- [x] Прогресс выполнения (какой tool используется)
- [x] Отметка сообщений как прочитанных

## 📋 TODO

### Фаза 4: Расширения
- [ ] MCP серверы (внешние)
- [ ] Custom Skills
- [ ] GitHub интеграция
- [ ] Голосовые сообщения

### Новые Tools
- [ ] `web_search` — поиск в интернете
- [ ] `send_telegram` — отправка сообщений
- [ ] `create_reminder` — напоминания с повторением
- [ ] `run_python` — выполнение Python кода

## 🚀 Запуск

```bash
cp .env.example .env
# Заполни .env

docker-compose build
docker-compose run --rm jobs
# При первом запуске: телефон + код, затем OAuth в браузере
```

## 📁 Структура

```
src/
├── config.py          # Конфигурация
├── main.py            # Точка входа
├── session.py         # Claude session
├── setup.py           # Первоначальная настройка
├── tools/
│   ├── __init__.py    # Регистрация tools
│   └── scheduler.py   # Scheduler tool
└── telegram/
    ├── client.py
    ├── auth.py
    └── handlers.py
```

## 🔧 Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TG_API_ID` | Telegram API ID | ✅ |
| `TG_API_HASH` | Telegram API Hash | ✅ |
| `TG_USER_ID` | Твой Telegram ID | ✅ |
| `ANTHROPIC_API_KEY` | API ключ (если без OAuth) | ❌ |
| `HTTP_PROXY` | Прокси для API | ❌ |

## 🛠 Добавление нового Tool

```python
# src/tools/my_tool.py
from claude_agent_sdk import tool

@tool("my_tool", "Description", {"arg": str})
async def my_tool(args):
    return {"content": [{"type": "text", "text": "Result"}]}

MY_TOOLS = [my_tool]
```

```python
# src/tools/__init__.py
from src.tools.my_tool import MY_TOOLS

ALL_TOOLS = [*SCHEDULER_TOOLS, *MY_TOOLS]
TOOL_NAMES = [..., "mcp__jobs__my_tool"]
```
